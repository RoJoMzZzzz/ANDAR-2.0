<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.research.andrade.andar">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.WRITE_SMS" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <uses-feature android:name="android.hardware.camera.flash" />

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".MessageContact" />
        <activity android:name=".MyQR" />
        <activity android:name=".NewQR" />
        <activity android:name=".ScanQR" />
        <activity android:name=".EmergencyContacts" />
        <activity android:name=".EditEmergencyContacts" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="101cb6b80263147729dc93e0fad2790cdb81c9c8" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Earth" />
        <activity android:name=".Hotlines" />
        <activity android:name=".Rate" />
        <activity android:name=".Fire" />
        <activity android:name=".Flood" />
        <activity android:name=".Landslide" />
        <activity android:name=".Tsunami" />
        <activity android:name=".Typhoon" />
        <activity android:name=".Volcano" />
        <activity android:name=".MessageContact2" />
        <activity android:name=".EmergencyHotlines2" />
        <activity android:name=".MessageLog" />
        <activity android:name=".CallLogs" />
        <activity android:name=".References" />
        <activity android:name=".About" />
        <activity android:name=".StrobeLightConfig" />
        <activity android:name=".EmergencyHotlines3"></activity>
    </application>

</manifest>